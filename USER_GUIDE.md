# 墨枢 · 使用手册

本手册面向写作用户，按页面讲清「怎么用」。首次使用请先看「一分钟上手」。

## 一分钟上手

1. 打开「设置」，填好模型接口（协议、Base URL、模型名、API Key），点测试确认连通。
2. 回到首页，点「新开一部」，写一句灵感。
3. 进入稿本工作台，按顺序执行：开书策划 → 世界观构建 → 人物小传 → 全书大纲 → 分章细纲。
4. 点「按细纲拆入目录」，把细纲拆成章节目录。
5. 逐章执行「章节正文」。卡住用「续写推进」，定稿前用「章节审稿」。
6. 想让文字更像自己，去「文风」页导入自己的原文，生成个人文风说明书。

## 设置页

填写当前供应商的连接信息：

- **协议**：OpenAI 兼容 / Anthropic / Ollama / Gemini
- **Base URL / 模型名 / API Key**
- **上下文长度 / 最大输出 / 温度**：按模型能力填
- **失败重试**：失败重试次数、退避基数、退避上限。只会对限流、服务端错误和网络错误重试，鉴权失败与安全拦截不会重试。

点「测试」验证连接，点「拉取模型」列出可用模型。可以保存多个供应商并在顶部切换。

## 首页

- 展示所有未归档稿本，显示标题、类型、字数与更新时间
- 新建稿本：填标题、类型、一句灵感
- 导入稿本：支持 Markdown 或工程 JSON
- 灵感开书：给一句想法，让系统先铺一版基础设定

## 稿本工作台

三栏结构：左边目录与设定，中间稿纸，右边手艺面板。

### 顶部与文件菜单

顶部可切换稿本、查看字数与保存状态、切换模型。文件菜单里可以保存工程、导出正文 Markdown、归档、复制、删除。

### 写作流程

1. **立项**：开书策划把灵感钉成「一句核 + 五句三灾 + 结局」。
2. **设定**：世界观、人物小传、关键道具、伏笔账本，都可单独生成或手改。
3. **大纲**：全书大纲先扩成卷纲。
4. **细纲**：分章细纲把卷纲节拍拆成可写的场面表。
5. **拆入目录**：把细纲按章拆进章节目录。
6. **正文**：逐章「章节正文」，按细纲场面顺序写；中断后用「续写推进」。
7. **审稿**：定稿前用「章节审稿」，按番茄标尺看钩子、爽点、人设与合规。

### 生成控制

- 生成时流式显示，可随时「停止」
- 生成前可勾选带入的上下文（设定、人物、细纲等）
- 可指定「这一章还想交代」作为额外要求
- 生成后自动写入对应字段；正文类会按模式替换或追加
- 「预览提示词」可在真正请求模型前，看这次会发出去什么、占用多少 token

### 自动保存与冲突

编辑后会自动保存。若同一部稿本在别处（例如另一个标签页）被改动，保存会提示冲突并暂停自动保存：此时你的改动与服务端的改动都已留档在 `data/conflicts/`，重新加载页面即可继续。

## 资产页（Skill）

这里是所有说明书的家。

- 浏览 21 个内置 Skill：写作类（开书策划、世界观、人物、大纲、分章细纲、章节正文、续写、润色、审稿、关键道具、伏笔账本、卡壳建议）与拆书类
- 克隆内置 Skill 成自定义 Skill，改正文与触发条件
- 单份 Markdown 导入导出，方便版本管理
- 查看历史与行级 diff，可回退到任意历史版本
- 管理「写作元素」：上下文锚定、只演不说、对白潜台词、五感落地、章末钩子等，按需启用或覆盖
- 技能详情里可「预览提示词」，用一个桩工程看拼装结果

自定义 Skill 的 front matter 支持：

- `tags`：元素与分类
- `whenFlow`：按流派触发
- `whenPlatform`：按平台触发
- `whenVoice`：按文风启用状态触发

## 文风页

个人文风（底味）优先级最高：一旦启用且已有说明书，作者的写法会压过平台通用文风规则。

- 导入样本：每篇至少 500 字，最多 60 篇
- 导入整本小说：按章自动切成独立样本，正文不截断
- 生成说明书：从你的原文逆向出句式、段长、标点、口头禅、称呼等习惯
- 迭代：给一段 AI 原文和你改后的版本，系统从差异里学你的口味
- 试笔：用当前说明书写一段，检查像不像
- 导入导出：说明书与样本可整体导出备份

## 拆书页

用于从参考小说里拆解写法（书源请使用作者自备的 TXT / Markdown / 粘贴文本，至少 500 字）。

1. 首页（`/teardown`）新建拆书工程或导入书源
2. 工作台（`/teardown/:id`）按章节切片
3. 逐项执行：拆书章纲、拆书角色、拆书黄金三章、拆书事件、拆书大纲、拆书细纲、拆书技法、仿写骨架
4. 默认只拆前 30 章，可在范围里调整

拆书结果与稿本分库存放，互不影响。

## 写作取向（系统默认遵守）

- **章首承接**：每章第一句必须接住上一章最后原文的动作、对话、物件或未完成的选择，这条压过细纲第一场。
- **只演不说**：情绪通过动作、身体、物件和没说出口的话来演。
- **一章一主冲突**：写完整一个来回，并留下后果。
- **标点与排版**：全角标点，一段连续逗号不超过 3 个，破折号一章最多 3 处；对话单独成段、用“”；系统输出一律【】且单独成段。
- **用字零容忍**：不得有错别字、同音别字、多字漏字、生造词，不得中英标点混用；人名、地名、功法前后写法必须一致。
- **底味优先**：个人文风启用时，作者的写法和偏好优先于平台通用规则。

## 常见问题

**模型连不上**：在设置页点「测试」；确认 Base URL 是否带 `/v1`、模型名是否正确、Key 是否有效。

**生成中途失败**：系统会对限流、服务端错误、网络错误自动重试。仍失败时看状态栏提示，鉴权与安全类错误需要检查配置或调整输入。

**保存冲突**：说明工程在别处被改过。两份内容都已留档在 `data/conflicts/`，重新加载页面后重试。

**想换回内置 Skill**：在资产页对已覆盖的技能点恢复即可，历史记录也在那里。

**数据在哪**：全部在项目 `data/` 目录，可直接备份整个目录。
